{
  "name": "user-app",
  "version": "0.1.0",
  "description": "Sistema de autorizacion de usuario (server & client)",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -t -n 'server,client' -c 'blue,green' \"npm run server\" \"npm run client\"",
    "server": "npm run server --prefix backend",
    "client": "npm run start --prefix frontend",
    "web": "npm run start --prefix web"
  },
  "author": "Pablo Funes",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.2"
  },
  "dependencies": {}
}
